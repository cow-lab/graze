DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="replace-with-a-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# Optional: enables live AI-generated explainers for research posts.
# Leave unset to run in demo mode (pre-written explainers for seed papers,
# and a placeholder explainer generated for new uploads).
ANTHROPIC_API_KEY=""
